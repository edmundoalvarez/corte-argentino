import styles from './Header.module.css';

function Header() {
    return (
        <header className={styles.header}>
                <h2>Shop on all our websites</h2>            
        </header>
    );
}

export default Header;
